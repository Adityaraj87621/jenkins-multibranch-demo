pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Building dev branch'
            }
        }

        stage('Test') {
            steps {
                echo 'Running Tests dev branch...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying Application dev branch '
            }
        }
    }
}
